---
description: Run just the watchlist screen refresh (Part B of weekly-review) on demand — use this to seed WATCHLIST.md the first time instead of waiting for Friday
---

Run STEP 6 through STEP 9 of `routines/weekly-review.md` only (the core
momentum+FCF screen, the satellite screener, the parallel Gemini Deep
Research catalyst confirmation, and writing the new lists to
memory/WATCHLIST.md). Skip the performance-review parts (STEP 1-5, 10) —
there's nothing to review yet on a first run.

Remember: submit all satellite catalyst research tasks first via
`scripts/gemini_research.sh submit`, collect the interaction ids, then poll
each one in a loop. Do not call blocking `research` per-candidate in serial
— with 10-15 candidates at 5-20 min each that would take hours instead of
minutes.

Budget 10-20 minutes for this to finish. Ask before committing
memory/WATCHLIST.md.
